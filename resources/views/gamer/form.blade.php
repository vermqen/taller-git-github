<x-layouts::app :title="$title">
    <div class="mx-auto max-w-3xl space-y-6 p-6 lg:p-10"><a class="text-sm font-semibold text-amber-600" href="{{ url()->previous() }}">Volver</a><div><p class="text-sm font-semibold uppercase tracking-[0.18em] text-amber-600">{{ $team->name }}</p><h1 class="mt-2 text-3xl font-bold">{{ $title }}</h1></div>
        <form method="POST" enctype="multipart/form-data" action="{{ isset($item) ? route($resource.'.update', [$team->slug, $item]) : route($resource.'.store', $team->slug) }}" class="space-y-5 rounded-2xl border border-zinc-200 bg-white p-6 dark:border-zinc-700 dark:bg-zinc-900">@csrf @isset($item) @method('PUT') @endisset
            @if ($resource === 'comentarios')<label class="block text-sm font-semibold">ID de publicación<input name="id_publicacion" type="number" value="{{ old('id_publicacion', $item->id_publicacion ?? '') }}" required class="mt-2 block w-full rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"></label>@endif
            @if ($resource === 'noticias')<label class="block text-sm font-semibold">Título<input name="titulo" value="{{ old('titulo', $item->titulo ?? '') }}" required maxlength="180" class="mt-2 block w-full rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"></label><label class="block text-sm font-semibold">Categoría<input name="categoria" value="{{ old('categoria', $item->categoria ?? '') }}" class="mt-2 block w-full rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"></label>@endif
            @if ($resource === 'noticias')<label class="block text-sm font-semibold">Foto de la noticia<input name="imagen" type="file" accept="image/jpeg,image/png,image/webp,image/gif" class="mt-2 block w-full rounded-lg border border-zinc-300 p-2 text-sm dark:border-zinc-600 dark:bg-zinc-800"></label><label class="block text-sm font-semibold">O pega el enlace de una imagen<input name="imagen_url" type="url" value="{{ old('imagen_url', $item->imagen_url ?? '') }}" placeholder="https://ejemplo.com/imagen.jpg" class="mt-2 block w-full rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"></label>@endif
            @if ($resource === 'comunidad')<label class="block text-sm font-semibold">Nombre<input name="nombre" value="{{ old('nombre', $item->nombre ?? '') }}" required maxlength="100" class="mt-2 block w-full rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"></label>@endif
            @if ($resource === 'problemas')<label class="block text-sm font-semibold">Título<input name="titulo" value="{{ old('titulo', $item->titulo ?? '') }}" required maxlength="160" class="mt-2 block w-full rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800"></label><div class="grid gap-4 sm:grid-cols-2"><label class="block text-sm font-semibold">Prioridad<select name="prioridad" class="mt-2 block w-full rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800">@foreach (['baja','media','alta','critica'] as $priority)<option @selected(old('prioridad', $item->prioridad ?? 'media') === $priority)>{{ $priority }}</option>@endforeach</select></label>@isset($item)<label class="block text-sm font-semibold">Estado<select name="estado" class="mt-2 block w-full rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800">@foreach (['abierto','en_progreso','resuelto','cerrado'] as $state)<option @selected(old('estado', $item->estado) === $state)>{{ $state }}</option>@endforeach</select></label>@endisset</div>@endif
            @if ($resource !== 'comentarios')<label class="block text-sm font-semibold">Descripción o contenido<textarea name="{{ $resource === 'noticias' ? 'contenido' : 'descripcion' }}" rows="8" required class="mt-2 block w-full rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800">{{ old($resource === 'noticias' ? 'contenido' : 'descripcion', $item->contenido ?? $item->descripcion ?? '') }}</textarea></label>@else<label class="block text-sm font-semibold">Comentario<textarea name="contenido" rows="6" required class="mt-2 block w-full rounded-lg border-zinc-300 dark:border-zinc-600 dark:bg-zinc-800">{{ old('contenido', $item->contenido ?? '') }}</textarea></label>@endif
            @if ($errors->any())<div role="alert" class="space-y-1 text-sm text-red-600"><p>{{ __('Please correct the following errors:') }}</p><ul class="list-disc ps-5">@foreach ($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
            <button type="submit" class="rounded-lg bg-amber-500 px-5 py-2 font-semibold text-zinc-950 hover:bg-amber-400">Guardar</button>
        </form>
    </div>
</x-layouts::app>